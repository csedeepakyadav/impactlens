# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Models (defaults shown) — "latest" aliases track Google's current stable models
ANALYSIS_MODEL=gemini-pro-latest
GUARD_MODEL=gemini-flash-latest

PORT=8787
